function App() {
  return (
    <main className="min-h-screen bg-slate-50 px-8 py-12 text-slate-900">
      <section className="mx-auto max-w-4xl rounded-2xl bg-white p-8 shadow-sm">
        <p className="mb-3 text-sm font-semibold uppercase tracking-wide text-blue-600">
          Personal Finance Tracker
        </p>

        <h1 className="text-3xl font-bold">
          Track your income, expenses, and monthly cash flow.
        </h1>

        <p className="mt-4 max-w-2xl text-base text-slate-600">
          A full-stack finance tracker for managing transactions, categories,
          and simple spending insights.
        </p>
      </section>
    </main>
  );
}

export default App;